# syntax=docker/dockerfile:1
FROM centos:7

WORKDIR /lokideploy

RUN yum -y install net-tools 

COPY . /lokideploy/

EXPOSE 3100

CMD ["/lokideploy/loki-linux-amd64","-config.file=/lokideploy/loki-local-config.yaml"]
